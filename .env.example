# .env.example — Copia in .env e riempi i valori. Non committare mai .env.

# URL del build hook Netlify (Dashboard → Site → Build hooks → "CMS Deploy")
# Necessario solo per la Netlify Function netlify/functions/deploy.js
NETLIFY_BUILD_HOOK_URL=https://api.netlify.com/build_hooks/TUO_HOOK_QUI
